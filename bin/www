#!/usr/bin/env node

/**
 * Module dependencies.
 */

var app = require('../app');
let custom_port = process.argv[2] || 3000;
app.listen(process.env.PORT || custom_port);

console.log('Express started on port ' + custom_port);
